# Root-Cause Protocol

Capture the exact denied tool, the requested path or command, and the active permission profile. Distinguish a true policy denial from a filesystem error such as [[eacces-on-dev-file-writes]].
