# Root-Cause Protocol

Check `/api/runner/status`, the latest task `status.md`, and runner logs. Record whether the three failures share the same CLI startup problem, task prompt blocker, or filesystem error.
