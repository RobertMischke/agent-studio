# Root-cause protocol

The diff summary collapsed to zero net lines even though the task history needed richer evidence about changed files and commits.

Protocol: search this entry before repeating diagnosis, then add task-specific logs and reproduction notes when a new occurrence appears.
