# 2026-04-15 — Baseline review

**Outcome:** Baseline OK.

## Scope

First pass over the local-only desktop product. Covered the backend HTTP surface (`/api/*`), the frontend rendering path, the watched-target read/write boundary, and the CLI driver layer.

## Findings

- HTTP listener bound to `localhost`; CORS limited to dev frontend origins. **Fine.**
- No secrets in tree (`grep` for `password|token|api[_-]?key` clean). **Fine.**
- Watched-target writes go through the agent task contract; the app does not patch source code on its own. **Fine.**
- Markdown rendering hand-written; no `[innerHTML]` outside the editor and the rendered docs panes. **Fine, with note R4 added.**

## Follow-ups

None blocking. Re-review when:

- The app starts accepting non-localhost connections, even behind a tunnel.
- The agent contract gains a path that lets the orchestrator write outside `<watchPath>/.orchestrator/jobs/*`.
- A new third-party rendering library is added to the frontend.
