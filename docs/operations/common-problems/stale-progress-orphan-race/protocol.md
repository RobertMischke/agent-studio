# Root-Cause Protocol

Read the task scanner log, crash recovery log, and lane transition events around the same timestamp. Confirm whether the folder was moved by the runner, crash recovery, stale-progress archiver, or a direct external write.
