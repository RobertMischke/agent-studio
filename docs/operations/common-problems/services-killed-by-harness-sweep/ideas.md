# Ideas

- Make repository lifecycle scripts report the owning process or service unit.
- Add a pre-flight warning when a known long-running service is launched as a
  session background task.
- Prefer targeted process-group or service-unit shutdown over name matching.
