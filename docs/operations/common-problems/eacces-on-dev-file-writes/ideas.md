# Ideas

- Add a small diagnostic script that prints backend status, recent watcher processes, and likely file-handle owners for a path.
- Document a Windows-specific retry pattern for CLI writes that fail with `EACCES`.
- Compare Claude's direct edit path with Codex and Copilot write behavior on the same file.
