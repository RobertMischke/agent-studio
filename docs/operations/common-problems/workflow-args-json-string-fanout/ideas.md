# Ideas

- Add reusable schema validation for workflow entry points.
- Require a declared maximum fan-out for every parallel workflow step.
- Surface pre-flight item and chunk counts in structured run evidence.
