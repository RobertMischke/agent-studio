// Frontend-only fallback ids for initial component state before the server
// catalog/defaults load. Selectable model lists still come from /api/cli.
export const CLAUDE_FALLBACK_MODEL_ID = 'claude-haiku-4-5';
