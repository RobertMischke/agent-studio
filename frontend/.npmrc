# Install file: dependencies (e.g. @coding-agent/chat) as physical copies
# instead of symlinks, so the bundler and vitest resolve a single copy of
# the Angular peer dependencies. Re-run `npm install` after rebuilding the
# library dist to pick up changes.
install-links=true
