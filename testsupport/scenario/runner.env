# Non-secret placeholder used only while Compose resolves the hermetic scenario
# override. The production runner services are moved to operator-runner there.
RUNNER_ID=deployment-scenario-disabled
RUNNER_GIT_REMOTE=/disabled
