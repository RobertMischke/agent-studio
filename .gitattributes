# This legacy benchmark source is stored as CRLF. Treat the carriage return as
# part of the line ending when checking newly added lines for whitespace.
backend.Tests/GitInfoPerfMeasurementTests.cs whitespace=cr-at-eol
