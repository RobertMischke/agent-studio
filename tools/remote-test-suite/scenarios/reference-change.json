{
  "$schema": "../scenario.schema.json",
  "version": 1,
  "name": "reference-change",
  "description": "Deterministic multi-file shipping quote change through remote claim, exact-SHA review, and integration.",
  "task": {
    "title": "Add deterministic priority shipping quotes",
    "body": "Add a priority shipping service. Validate destination zones, apply the declared per-zone base and per-item rates, round once to cents, expose the service from the package entry point, document it, and add semantic tests. Finish with [[TASK_DONE]].",
    "key": "RTS-1"
  },
  "fixture": {
    "defaultBranch": "develop",
    "changeCommand": ["node", "{suiteRoot}/apply-reference-change.mjs", "--seed", "{seed}"],
    "acceptanceCommand": ["node", "--test", "test/*.test.mjs"],
    "expectedChangedFiles": [
      "README.md",
      "package.json",
      "src/index.mjs",
      "src/shipping.mjs",
      "test/shipping.test.mjs"
    ]
  },
  "phases": ["claim", "run", "gate", "review", "integration"],
  "resources": {
    "workspaceId": "wsp-{runId}",
    "projectId": "prj-{runId}",
    "taskId": "tsk-{runId}"
  },
  "hooks": {}
}
